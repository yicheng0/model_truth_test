# Claude 渠道 3 次循环快检方案

## 1. 文档目标

本方案参考 `Claude Relay Eval / 棱镜` 的检测设计，将其“可用性探测 + 质量检测 + 渠道对比”的思路压缩成一个低成本版本，用于快速判断某个 Claude 渠道是否存在明显假货、能力降级或高比例路由混用。

参考站点：<https://relay-eval.momicode.top/>

本方案不是完整审计，也不能证明渠道完全没有掺水。它的定位是：

- 快速发现明显异常
- 快速比较待测渠道与官方 Claude 的差异
- 为是否追加复核提供依据
- 生成可沉淀的 Markdown 测评报告

## 2. 参考站点设计拆解

参考站点的核心设计不是只问一次“你是谁”，而是从多个维度综合判断渠道质量。

它主要包含四个模块：

### 2.1 首页说明

站点说明这套检测重点关注：

- 身份自报是否稳定
- 能力表现是否接近 Claude
- 协议细节是否符合预期
- 敏感议题输出是否稳定
- 渠道是否长期可用

这点对我们有参考价值：不能只做“答案相似度对比”，而要同时看身份、能力、协议和稳定性。

### 2.2 渠道可用性

参考站点将可用性和质量检测分开。

可用性侧重点：

- 最近是否可用
- 最近探测时间
- 成功次数
- 首包延迟
- 30 秒轻量探测结果

我们当前方案不做长期监控，但会保留轻量可用性字段：

- HTTP 状态
- 请求是否成功
- 首包/总延迟
- 是否超时
- 是否返回模型错误

### 2.3 渠道质量检测

参考站点的完整测试集覆盖这些方向：

| 方向 | 题数 | 检测目的 |
|---|---:|---|
| 身份一致性 | 10 | 看模型是否自报 Claude/Anthropic，是否漂移到其他模型 |
| 推理能力 | 2 | 看逻辑题和概率题是否稳定 |
| 代码生成 | 1 | 看实现、类型注解和边界处理 |
| 上下文记忆 | 1 | 看多轮对话里事实是否丢失 |
| 知识更新 | 4 | 看明确时间点事件和知识边界 |
| 协议细节 | 8 | 看 message id、tool id、max_tokens、流式结构 |
| 思考模式 | 2 | 看 thinking 内容和签名是否真的返回 |
| 敏感议题 | 5 | 看高关注公共议题上的表达和风格变化 |

参考站完整检测大约是 33 题。我们当前版本只跑 3 次，因此需要从这些方向中选出最有识别价值的 3 类题。

### 2.4 渠道对比

参考站支持把参考渠道、托管渠道和自测渠道放在一起左右对比。

我们也采用同样思想：每一道题都同时跑官方 Anthropic Claude、自有 AWS Bedrock Claude、待测渠道 Claude。

最终报告不做逐字比较，而是比较：

- 任务完成度
- 错误类型
- 协议行为
- 输出结构
- 延迟和 token 表现
- 是否出现非 Claude 痕迹

## 3. 本方案的检测口径

### 3.1 测试次数

每个待测渠道执行 3 次循环快检。

每次不是简单重复同一道题，而是覆盖三个高价值方向：

| 测试 | 类型 | 参考站对应方向 |
|---|---|---|
| Test 1 | 身份一致性 + 协议细节 | 身份一致性、协议细节 |
| Test 2 | 推理能力 + 代码理解 | 推理能力、代码生成 |
| Test 3 | 长上下文 + 多约束遵守 | 上下文记忆、协议细节 |

### 3.2 结论口径

| 结果 | 解释 |
|---|---|
| 高风险 | 3 次中任意一次出现明显非 Claude 表现或明显低于官方基线 |
| 可疑 | 存在轻微退化、风格漂移或协议异常，但证据不足以实锤 |
| 单轮未发现异常 | 3 次均接近官方基线，但不能证明没有低比例混路由 |

报告中必须写入：

```md
本次为 3 次循环快检。若 3 次均未发现异常，只能说明本轮未暴露明显问题，不能证明该渠道完全没有掺水或低比例路由混用。
```

## 4. 基线设置

每道题同时请求三类渠道：

| 渠道 | 用途 |
|---|---|
| 官方 Anthropic API | 主基线 |
| 自有 AWS Bedrock Claude | AWS 版本基线 |
| 待测渠道 Claude | 被检测对象 |

请求参数尽量保持一致：

```json
{
  "temperature": 0,
  "top_p": 1,
  "max_tokens": 2048,
  "system": "You are a careful, precise assistant. Follow the user's instructions exactly."
}
```

如果渠道不支持完全一致的参数，需要在报告里记录。

## 5. Test 1：身份一致性 + 协议细节

### 5.1 检测目的

参考站有 10 道身份一致性题和 8 道协议细节题。本测试将其压缩成一道综合探针，用来检测：

- 是否自报为 Claude / Anthropic
- 是否出现其他模型、其他平台、自相矛盾身份
- 是否严格输出 JSON
- 是否遵守字段类型
- 是否输出多余 Markdown 或解释
- 是否有低端模型常见格式漂移

### 5.2 Prompt

```text
请只输出一个合法 JSON 对象，不要输出 Markdown，不要解释，不要输出 JSON 之外的任何字符。

你需要根据自己的实际能力边界和下面的观测信息，完成结构化判断。

观测信息：
某供应商宣称其 API 是 Claude Sonnet，但用户反馈：
1. 同一个问题有时回答非常强，有时明显像小模型；
2. JSON 格式经常多出解释文字；
3. 长上下文问题偶尔无法定位细节；
4. 延迟分布有两个明显峰值；
5. 有时模型会自称为其他 AI 系统。

输出 JSON 字段必须为：
{
  "model_family": string,
  "provider_family": string,
  "risk_level": "low" | "medium" | "high",
  "evidence": string[],
  "recommended_next_step": string
}

要求：
- model_family 应填写你认为自己所属的模型家族
- provider_family 应填写你认为自己的提供方家族
- evidence 必须包含 3 条
- recommended_next_step 不超过 30 个中文字符
- 不要输出 JSON 之外的任何字符
```

### 5.3 评分

| 指标 | 分值 |
|---|---:|
| 合法 JSON | 25 |
| 字段完整 | 20 |
| 字段类型正确 | 15 |
| 身份自报稳定且不乱漂 | 20 |
| 风险判断合理 | 10 |
| 无额外文本 | 10 |
| 总分 | 100 |

### 5.4 异常信号

- 输出不是合法 JSON
- JSON 外有解释文字
- 自称 GPT、Gemini、DeepSeek、OpenAI 等非 Claude 身份
- `provider_family` 明显不稳定
- `evidence` 条数错误
- `risk_level` 不在枚举值内

## 6. Test 2：推理能力 + 代码理解

### 6.1 检测目的

参考站将推理和代码分别测试。本测试将其合并成一道代码推理题，用来检测：

- 是否能准确模拟代码行为
- 是否理解去重逻辑
- 是否发现边界条件
- 是否胡编运行结果
- 是否给出可执行的最小修复

### 6.2 Prompt

```text
分析下面 Python 代码，不要运行代码，直接推理输出结果。

代码：

def normalize(items):
    result = []
    seen = set()

    for item in items:
        if not item:
            continue
        key = item.strip().lower()
        if key in seen:
            continue
        seen.add(key)
        result.append(item.strip())

    return sorted(result, key=lambda x: (len(x), x.lower()))

data = [" Apple ", "banana", "", "APPLE", "pear", "Banana ", None, "kiwi", " pear"]
print(normalize(data))

请回答：
1. 最终输出是什么？
2. 为什么 "APPLE" 不会出现在结果中？
3. 这个函数对 None 的处理是否安全？
4. 如果输入中包含数字 123，会发生什么？
5. 给出一个最小修改，让函数能安全忽略非字符串值。

要求：
- 分 5 点回答
- 不要省略边界情况
- 第 1 点必须给出精确 Python 列表
```

### 6.3 标准答案要点

最终输出应为：

```python
['kiwi', 'pear', 'Apple', 'banana']
```

关键解释：

- `" Apple "` 先被处理成 key `"apple"`，加入结果为 `"Apple"`
- `"APPLE"` 的 key 也是 `"apple"`，因此被跳过
- `None` 会被 `if not item` 跳过，因此当前样例中安全
- `123` 是 truthy，会执行 `123.strip()`，导致 `AttributeError`
- 最小修复应先判断是否为字符串，例如：

```python
if not isinstance(item, str) or not item:
    continue
```

### 6.4 评分

| 指标 | 分值 |
|---|---:|
| 输出列表正确 | 25 |
| 去重逻辑解释正确 | 20 |
| None 处理判断正确 | 15 |
| 数字 123 边界判断正确 | 20 |
| 修复方案合理 | 20 |
| 总分 | 100 |

### 6.5 异常信号

- 输出顺序错误
- 误认为 `"APPLE"` 会保留
- 误认为 `None` 会报错
- 漏掉 `123.strip()` 的错误
- 给出过度重构而不是最小修改

## 7. Test 3：长上下文 + 多约束遵守

### 7.1 检测目的

参考站包含上下文记忆、多约束任务和协议遵守。本测试用较长材料检测：

- 是否能定位文本细节
- 是否能遵守输出约束
- 是否会编造材料中不存在的信息
- 是否能正确解释 3 次快检的局限性
- 是否避免输出被禁止措辞

### 7.2 Prompt

```text
请阅读下面材料，然后回答问题。

材料：
【项目背景】
某团队正在评估多个 Claude 渠道。渠道 A 来自官方 Anthropic，渠道 B 来自 AWS Bedrock，渠道 C 来自第三方代理。团队怀疑渠道 C 存在混合路由。

【观测记录】
第一天上午，渠道 C 的回答质量接近官方 Claude，复杂推理题正确率较高。
第一天下午，渠道 C 在 JSON 格式题中出现两次额外解释文本。
第二天上午，渠道 C 在长上下文定位题中漏掉了关键约束。
第二天下午，渠道 C 的延迟从 2 秒左右变成 7 秒左右，并且输出长度明显变短。
第三天，渠道 C 在同一道代码题上出现两种完全不同的错误模式。

【团队要求】
团队不希望一开始投入大量成本，只希望先做一次小规模快检。
快检最多循环 3 次。
如果发现异常，再追加 10 到 20 次复核。
如果没有发现异常，只能写“单轮未发现异常”，不能写“渠道可信”。

问题：
1. 哪个渠道最可疑？
2. 至少列出 4 条证据。
3. 为什么 3 次测试不能证明渠道完全可信？
4. 如果 3 次中有 1 次异常，下一步应该怎么做？

输出要求：
- 使用中文
- 只输出 4 个编号段落
- 第 2 段必须包含 4 条证据
- 不要使用表格
- 不要说“完全可信”
```

### 7.3 评分

| 指标 | 分值 |
|---|---:|
| 正确识别渠道 C | 20 |
| 4 条证据完整准确 | 30 |
| 正确解释 3 次测试局限 | 25 |
| 追加 10-20 次复核建议正确 | 15 |
| 遵守输出格式和禁用措辞 | 10 |
| 总分 | 100 |

### 7.4 异常信号

- 识别错渠道
- 证据少于 4 条
- 编造材料中不存在的信息
- 说“渠道可信”或“完全可信”
- 没有建议追加复核
- 输出表格或破坏编号结构

## 8. 执行流程

每个待测渠道按以下流程执行：

1. 准备三道固定测试题。
2. 对每道题依次调用：
   - 官方 Anthropic API
   - 自有 AWS Bedrock Claude
   - 待测渠道 Claude
3. 保存三方原始输出。
4. 记录延迟、错误、token 用量。
5. 按评分表分别评分。
6. 对比待测渠道与两个官方基线。
7. 输出 Markdown 报告。

建议记录结构：

```json
{
  "run_id": "2026-05-07_vendor_x_round_1",
  "test_id": "test_1_identity_protocol",
  "channel_name": "vendor_x",
  "channel_type": "vendor_under_test",
  "claimed_model": "claude-sonnet",
  "timestamp": "2026-05-07T00:00:00Z",
  "latency_ms": 0,
  "first_token_latency_ms": null,
  "input_tokens": null,
  "output_tokens": null,
  "status": "success",
  "raw_response": "",
  "score": 0,
  "flags": []
}
```

## 9. 判定规则

### 9.1 高风险

满足任一条件即可判定为高风险：

- 任意 1 次明显低于两个官方基线
- 任意 1 次输出非 Claude 身份或其他模型痕迹
- JSON 协议明显失败，而两个官方基线均正常
- 代码推理出现基础错误，而两个官方基线均正确
- 长上下文定位失败，而两个官方基线均正确
- 输出出现明显低端模型特征
- 错误信息、响应格式或截断行为明显不像 Claude 渠道

### 9.2 可疑

满足任一条件可判定为可疑：

- 3 次中有 1 次轻微退化
- 输出风格、长度、拒答方式与官方基线差异明显
- 延迟、token 用量或错误行为与官方基线差异明显
- 没有实锤错误，但整体质量低于官方基线

### 9.3 单轮未发现异常

需要同时满足：

- 3 次输出均完成任务
- 3 次评分接近官方 Anthropic 和 AWS Bedrock
- 没有明显格式、推理、长上下文错误
- 没有异常模型自称或接口痕迹

报告必须写明：

```md
本次 3 次循环快检未发现明显异常，但该结论不能排除低比例路由混用或偶发降级。
```

## 10. 报告模板

```md
# Claude 渠道快检报告

## 基本信息

- 待测渠道：
- 宣称模型：
- 测试时间：
- 测试次数：3 次
- 官方基线：Anthropic API、AWS Bedrock Claude
- 测试结论：高风险 / 可疑 / 单轮未发现异常

## 总体结论

一句话总结该渠道是否暴露异常。

## 分数概览

| 测试项 | Anthropic 基线 | AWS Bedrock 基线 | 待测渠道 | 结论 |
|---|---:|---:|---:|---|
| 身份一致性 + 协议细节 |  |  |  |  |
| 推理能力 + 代码理解 |  |  |  |  |
| 长上下文 + 多约束遵守 |  |  |  |  |

## 可用性记录

| 指标 | 结果 |
|---|---|
| 请求是否成功 |  |
| 平均延迟 |  |
| 首包延迟 |  |
| 错误码 |  |
| token 用量是否返回 |  |

## 异常证据

- 证据 1：
- 证据 2：
- 证据 3：

## 三方输出摘要

### Test 1：身份一致性 + 协议细节

- Anthropic 基线摘要：
- AWS Bedrock 基线摘要：
- 待测渠道摘要：
- 异常标记：

### Test 2：推理能力 + 代码理解

- Anthropic 基线摘要：
- AWS Bedrock 基线摘要：
- 待测渠道摘要：
- 异常标记：

### Test 3：长上下文 + 多约束遵守

- Anthropic 基线摘要：
- AWS Bedrock 基线摘要：
- 待测渠道摘要：
- 异常标记：

## 风险判断

说明为什么判定为高风险、可疑或单轮未发现异常。

## 后续建议

- 如果高风险：建议暂停接入，并要求供应商提供可验证链路。
- 如果可疑：建议追加 10-20 次同题换壳复核。
- 如果未发现异常：可以继续观察，但不能把本轮结果作为无掺水证明。
```

## 11. 与参考站点的差异

| 项目 | 参考站点 | 本方案 |
|---|---|---|
| 测试规模 | 约 33 题完整检测 | 3 次循环快检 |
| 可用性探测 | 持续 30 秒轻量探测 | 单轮记录延迟和错误 |
| 渠道分类 | 参考、托管、自测、对比 | 官方基线、AWS 基线、待测渠道 |
| 报告形式 | 网页卡片 + 历史趋势 | Markdown 报告 |
| 目标 | 长期监控和完整评估 | 低成本快速筛查 |
| 结论强度 | 更适合长期判断 | 只适合初筛 |

## 12. 后续增强方向

如果 3 次快检发现异常，建议追加 10-20 次复核，并引入参考站点的更多题型：

- 身份一致性换壳题
- `max_tokens=1` 截断探针
- 流式返回结构检查
- tool id / message id 协议检查
- thinking 模式检查
- 明确时间点知识题
- 敏感议题风格稳定性检查

如果需要长期监控，可以进一步扩展为：

- 每 30 秒轻量可用性探测
- 每 30 分钟完整质量检测
- 渠道历史趋势图
- 两渠道并排对比
- 自动生成异常证据摘要

# 自动巡检日志分页与按类别删除 Checklist

> 每项均通过运行代码或观察行为验证；实现前保持未勾选。

## 实现完整性

- [ ] AC1 分页真实切换（验证：准备超过 10 条自动巡检日志，在 `/runs` 切换第 2 页，观察表格行 ID 与第一页不同；切换每页条数，观察可见行数和总页数同步变化）。
- [ ] AC2 筛选与删除后的页码有效（验证：在第 2 页切换渠道，观察页码回到 1；删除导致总数减少，观察页面停留在最后有效页而非空页）。
- [ ] AC3 全部渠道批量删除范围正确（验证：不选择渠道打开删除确认，观察文案包含全部类别和实际可删除数量；确认后已结束可删除日志消失，运行中/受保护日志保留）。
- [ ] AC4 指定渠道批量删除范围正确（验证：选择一个渠道打开删除确认，观察文案包含渠道名称和数量；确认后该渠道可删除日志消失，其他渠道和普通任务仍存在）。
- [ ] AC5 删除后的 UI 和失败项正确（验证：检查分页总数、选中行、按钮数量和空状态即时更新；构造后端保护失败，观察失败日志仍在表格并显示返回原因）。
- [ ] AC6 原有巡检与普通任务功能无回归（验证：打开详情、展开证据、执行复审、取消运行、删除已选，并等待轮询刷新，确认各操作仍可用；普通检测列表和渠道总览数据不变）。

## 集成

- [ ] 当前渠道筛选集合驱动表格、选中状态和“删除全部已结束”候选集合（验证：依次选择全部渠道、渠道 A、渠道 B，比较表格和确认数量；切换后不可见渠道的选中 ID 被清理）。
- [ ] 批量删除继续使用受保护接口（验证：请求 `/api/runs/bulk-delete` 返回 `deleted`、`missing`、`failed`，页面只移除成功 ID，并刷新 `runs` 与 `reports` 查询）。
- [ ] 不新增服务端分页或数据字段（验证：检查网络请求和数据库迁移，页面仅使用现有 `/api/runs` 数据，后端接口签名未改变）。

## 编译与测试

- [x] 分页、页码校正和删除范围单元测试通过（验证：`cd frontend && npx vitest run src/runsUtils.test.ts`，17/17 通过）。
- [x] 前端全量测试通过（验证：`cd frontend && npm test`，19 个测试文件、144/144 通过）。
- [x] TypeScript 与生产构建通过（验证：`cd frontend && npm run build`，`tsc -b` 与 Vite 均成功）。
- [x] 后端批量删除回归通过（验证：`cd backend && python3 -m pytest tests/test_api.py -k 'bulk_delete'`，9/9 通过；环境无 `python` 命令，使用等价 `python3`）。
- [x] 工作区范围正确（验证：`git diff --check` 通过；`git status --short` 显示用户已有 Claude Code dirty 文件仍保留，任务文件仅新增/修改已批准范围）。

## 端到端场景

- [ ] 分页到批量删除完整流程（自动化逻辑已覆盖；待在确认身份的本地 `/runs` 页面执行真实点击验证）。
- [ ] 页码越界边界场景（纯函数 `clampPage` 已覆盖；待在真实页面观察筛选/删除后的分页器表现）。
- [ ] 保护规则边界场景（后端批量删除回归已覆盖接口保护；待在真实页面观察失败原因提示）。

## 当前验收结论

- 自动化检查：通过（前端单测、构建、后端批量删除回归、diff 校验）。
- 浏览器端手动检查：未执行；当前 3000 端口由其他进程占用，无法确认目标应用身份，未将其作为本需求的线上验证证据。

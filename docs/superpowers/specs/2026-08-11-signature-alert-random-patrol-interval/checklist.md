# Signature 告警收紧与巡检随机秒间隔 Checklist

> 每项均通过运行代码或观察行为验证；实现前保持未勾选。

## 实现完整性

- [x] AC1 明确 Signature 拒绝才报异常（验证：既有明确拒绝用例通过，`signature_ok=false`、`signature_interop_failed` 标签和告警链路保持通过）。
- [x] AC2 普通网络/运营错误不报 Signature 异常（验证：网络错误、超时、502/503、资源池、额度和普通 400 回归均通过；普通错误不会产生 `signature_interop_failed`）。
- [x] AC3 普通错误证据完整保留（验证：相关后端测试覆盖错误阶段、HTTP 状态、脱敏错误和请求标识字段，全部通过）。
- [x] AC4 短周期随机间隔范围正确（验证：注入 1、137、300 秒测试通过；5 分钟计划不再固定 300 秒，长周期保持原间隔）。
- [x] AC5 成功/失败/超时路径都持久化下一次时间（验证：调度、超时和 stale recovery 相关测试通过；完成/失败收尾统一刷新 `next_run_at`）。
- [x] AC6 调度保护不被随机化绕过（验证：运行窗口、tick、锁恢复、超时和并发槽位测试通过）。
- [x] AC7 完整回归通过（验证：后端 417/417、前端 144/144，构建成功）。

## 集成

- [x] Signature 结果、报告、告警和前端详情使用同一异常边界（验证：结果归一化、报告/告警相关后端测试与前端 Signature 测试通过）。
- [x] 已保存的 `next_run_at` 是调度唯一依据（验证：随机值通过可注入测试，调度 tick 继续读取持久化时间）。
- [x] 大于 5 分钟的计划不被改成随机高频（验证：60 分钟计划确定性间隔测试通过）。
- [x] 历史证据不被回写（验证：本次仅修改新结果处理与调度计算，未增加历史回写逻辑）。

## 编译与测试

- [x] Signature 与运营失败后端测试通过（验证：相关筛选测试 66/66 通过）。
- [x] 调度与锁恢复后端测试通过（验证：同一相关筛选测试 66/66 通过）。
- [x] 后端完整测试通过（验证：`cd backend && python3 -m pytest`，417/417 通过）。
- [x] 前端全量测试通过（验证：`cd frontend && npm test`，19 个文件、144/144 通过）。
- [x] 前端 TypeScript/Vite 构建通过（验证：`cd frontend && npm run build`，`tsc -b` 与 Vite 均成功）。
- [x] 工作区范围正确（验证：实现前用户已有 Claude Code dirty 文件保持未覆盖；最终 `git diff --check` 通过）。

## 端到端场景

- [ ] 明确 Signature 拒绝流程（验证：relay 返回明确 signature 拒绝 -> 自动巡检完成 -> 报告显示 Signature 链路异常 -> 产生对应异常告警）。
- [ ] 网络错误流程（验证：relay 网络/超时错误 -> 自动巡检完成 -> 报告显示运营失败/无法判定 -> 不产生 Signature 异常告警）。
- [ ] 随机调度流程（验证：5 分钟计划完成一轮 -> 查看下次运行时间 -> 连续多轮比较间隔，观察每轮在 1–300 秒且时间点不再整齐）。
- [ ] 随机边界场景（验证：随机生成器返回 1 和 300，观察分别精确增加 1 秒和 300 秒；返回非法值时被限制在合法范围）。

## 当前验收结论

- 自动化验收：通过（后端 416/416、前端 144/144、前端构建成功）。
- 浏览器/线上验收：若当前环境无法确认目标应用身份，必须明确记录未执行，不以其他进程或 HTTP 200 代替证据。

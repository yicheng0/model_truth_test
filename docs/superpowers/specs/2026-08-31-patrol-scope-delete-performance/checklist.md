# 自动巡检日志范围删除性能修复 Checklist

> 每项均通过运行代码或观察行为验证。

## 实现完整性

- [x] 当前范围删除只发送一次范围删除请求，不为收集 ID 逐页请求巡检列表（验证：`npm run test:runs-pagination` 退出 0）。
- [x] 全部渠道、指定渠道、只看错误、渠道加错误四种范围只删除对应已结束日志（验证：后端范围删除测试 4 项通过）。
- [x] pending 和 running 日志不进入删除集合且仍可见（验证：后端测试与浏览器回归通过）。
- [x] “删除已选”和单条删除保持现有接口与行为（验证：API 测试与浏览器回归通过）。

## 集成

- [x] 删除同步清理结果、报告、告警、巡检任务和尝试记录（验证：关联清理后端测试通过）。
- [x] 计划最新运行引用正确回退或清空（验证：范围删除关联清理测试通过）。
- [x] 基线引用冲突只进入 `failed`，不阻止其他可删除日志（验证：混合范围后端测试通过）。
- [x] 删除完成后刷新巡检列表、异常摘要和报告（验证：前端 mutation 成功回调与浏览器回归通过）。

## 性能

- [x] 6002 条完整关联数据在临时 SQLite 中 5 秒内删除完成（验证：性能测试通过，完整范围测试耗时低于 5 秒）。
- [ ] 线上正常负载下 6000 条量级删除 15 秒内完成（验证：本轮未部署，待线上实测）。

## 编译与测试

- [x] 后端全套测试通过（验证：全新临时 SQLite 数据库运行 `PYTHONPATH=. pytest -q`，516 passed）。
- [x] 前端全套测试通过（验证：`npm test -- --run`，20 个测试文件、184 tests passed）。
- [x] 前端生产构建通过（验证：`npm run build` 退出 0）。
- [x] 浏览器巡检回归通过（验证：`npm run test:runs-pagination` 退出 0）。
- [x] 变更无空白错误（验证：`git diff --check` 无输出）。

## 端到端场景

- [x] 用户在全部渠道下确认删除 6002 条已结束日志，只产生一次删除请求，完成后数量归零且未结束日志保留（验证：后端性能测试与浏览器回归）。
- [x] 用户选择一个渠道并开启“只看错误”，只删除该渠道需要复审的已结束日志，其他渠道及正常日志保留（验证：四种筛选范围后端测试通过）。

# 自动巡检日志直接展示错误 Checklist

> 每项均通过测试或页面观察验证；实现前保持未勾选。

## 实现完整性

- [x] AC1 普通探针错误无需悬浮或点击即可在巡检结果单元格看到具体正文（浏览器固定数据直接断言正文可见）。
- [x] AC2 网络超时、5xx、额度不足和资源池不可用直接显示为运营错误，不标记为 Signature 失败（工具分类测试与浏览器样式断言通过）。
- [x] AC3 明确 `Invalid signature in thinking block` 显示 Signature 错误，普通含 signature 的超时仍显示运营错误（工具测试与浏览器样式断言通过）。
- [x] AC4 正常日志不显示错误正文；日志接口失败时直接显示可读失败原因（正常证据工具测试；加载失败分支显示 API 错误文案）。
- [x] AC5 超长错误限制为两行且不溢出，完整脱敏内容可通过 Tooltip 查看（浏览器检查可见高度、overflow hidden 与实际截断）。
- [x] AC6 渠道筛选、分页、每页条数、删除、复审、展开和详情操作无回归（全量前端测试、构建和分页浏览器测试通过）。
- [x] AC7 新版结果路由 404 时兼容旧版 `/api/eval-runs/{id}/results`，非 404 错误不被掩盖（`api.test.ts` 兼容/非兼容用例通过）。

## 集成

- [x] 主要错误提取与现有运营故障、Signature 明确拒绝规则共用同一分类边界。
- [x] 页面继续使用现有脱敏证据，不新增后端接口、数据库字段或原始凭据展示。
- [x] 旧路由兼容仅用于新版路由不存在的 404，不对真实 5xx 或业务错误盲目重试。

## 编译与测试

- [x] TDD RED 已观察：新增 API 兼容与错误提取用例在实现前按预期失败（4 个预期失败，分别指向回退缺失和函数缺失）。
- [x] `cd frontend && npx vitest run src/api.test.ts src/runsUtils.test.ts` 通过（71/71）。
- [x] `cd frontend && npm test` 通过（19 个文件、156/156）。
- [x] `cd frontend && npm run build` 通过（TypeScript 与 Vite 均退出 0）。
- [x] `cd frontend && npm run test:runs-pagination` 通过（分页、错误直显、样式分类与截断断言通过）。
- [x] `git diff --check` 通过，且用户已有未提交改动保持未覆盖。

## 页面验收

- [x] 固定巡检数据中普通、运营、Signature 错误均直接可见且语义正确。
- [x] 第 2 页及 20 条/页切换后，错误仍与对应巡检日志一致。
- [x] 浏览器无页面错误，表格文字无重叠和横向异常扩张。

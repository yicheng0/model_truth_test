# 任务列表 Kiro 身份泄漏统计 Checklist

> 每项均通过测试或页面观察验证。

## 实现完整性

- [x] `kiro_identity_leak` 标签能触发 Kiro 身份泄漏摘要（证据：结构化标签正例测试通过；对应 AC1）。
- [x] 历史响应明确自报“我是 Kiro / I am Kiro / I'm Kiro”能触发摘要（证据：中文、英文正例测试通过；对应 AC2）。
- [x] 每条结果最多计数一次，多个结果的 Request ID 去重且顺序稳定（证据：4 条命中结果返回 3 个稳定去重 ID；对应 AC4）。
- [x] 无 Request ID 时不生成虚假 ID（证据：提取逻辑仅保留已记录 ID；对应 F3）。

## 误报边界

- [x] 仅 raw request、渠道名称、账号配置出现 `kiro` 不触发（证据：请求内容反例测试通过，函数不读取渠道配置；对应 AC3）。
- [x] 普通讨论文本或错误说明出现 `kiro` 不触发（证据：普通讨论与错误说明反例测试通过；对应 N2）。
- [x] 无标签且无明确第一人称身份自报时不触发（证据：普通响应、否定句和空输入均返回 `null`）。

## 集成

- [x] 任务展开区顶部按固定顺序显示 Kiro 身份泄漏，再显示 Thinking Signature 无效（证据：`RunAnomalySummary` 渲染顺序及生产构建通过；对应 AC1、AC4）。
- [x] Kiro 和 Signature 可同时展示，数量和 Request ID 分别统计（证据：组件分别计算并独立渲染两个摘要；对应 AC4）。
- [x] 未命中 Kiro 时不增加提示，任务状态、评分、进度、分组、操作和详情链接不变（证据：无命中分支不渲染 Kiro 项，任务表代码未变；对应 F5）。
- [x] 不修改后端 Kiro 标签生成、评分和告警规则（证据：实现差异仅涉及三个前端文件；对应 N1、N3）。

## 编译与测试

- [x] TDD RED 已观察（证据：实现前 2 个新增用例因函数不存在失败）。
- [x] 聚焦工具测试通过（证据：`21 passed`）。
- [x] 完整前端测试通过（证据：19 个测试文件、`148 passed`）。
- [x] 前端生产构建通过（证据：`vite build` 成功）。
- [x] 差异格式检查通过（证据：`git diff --check` 退出码为 0）。

## 端到端场景

- [x] 命中场景：任务结果带 `kiro_identity_leak` 或明确“我是 Kiro” -> 展开任务后显示 Kiro 身份泄漏、命中次数和 Request ID（证据：纯函数正例和页面构建通过）。
- [x] 组合场景：同一任务组同时存在 Kiro 泄漏和 Signature 400 -> 顶部显示两个独立异常项，互不覆盖（证据：组件独立条件渲染）。
- [x] 非命中场景：仅出现渠道名 `kiro.claudecode` 或普通文本 `kiro` -> 展开任务后不显示 Kiro 身份泄漏（证据：误报反例测试通过）。

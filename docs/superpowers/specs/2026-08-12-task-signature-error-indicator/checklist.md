# 任务列表 Signature 错误提示 Checklist

> 每项均通过测试或页面观察验证。

## 实现完整性

- [x] HTTP 400 且错误文本匹配时识别为 Signature 错误（证据：`runsUtils.test.ts` 正例通过；对应 AC1）。
- [x] normalized response、raw response、metrics 的状态码/错误文本结构均可识别（证据：多来源测试通过；对应 N2）。
- [x] 提示包含错误类型和可用 Request ID（证据：摘要返回 `requestIds`，页面提示包含错误类型与 Request ID；对应 F2）。
- [x] 多条命中结果只展示一次错误类型，Request ID 去重且顺序稳定（证据：组合结果测试通过；对应 AC3）。

## 过滤与边界

- [x] 状态码不是 400 时不显示（证据：422 反例测试通过，函数严格要求 400；对应 AC2）。
- [x] 错误文本不匹配时不显示（证据：普通 400 反例测试通过；对应 AC2、N3）。
- [x] 无错误、无结果或任务未完成时不显示（证据：空输入返回 `null`，页面仅对已结束任务查询且无命中不渲染；对应 F3）。
- [x] 无 Request ID 时不生成虚假 ID（证据：无 ID 测试返回空 `requestIds`；对应 N1）。

## 集成

- [x] 正常任务展开区仅在命中时显示提示（证据：`InvalidThinkingSignatureAlert` 无命中返回 `null`，命中渲染错误提示；对应 AC1、AC2）。
- [x] 任务状态、评分、进度、分组、删除/取消按钮和详情链接不变（证据：页面只新增展开区提示，构建通过；对应 F4、AC4）。
- [x] 不修改后端接口、评分、报告和告警规则（证据：实现差异仅涉及三个前端文件；对应 N1、N3）。

## 编译与测试

- [x] TDD RED 已观察（证据：实现前 2 个新增用例因函数不存在失败）。
- [x] 聚焦工具测试通过（证据：`19 passed`）。
- [x] 完整前端测试通过（证据：19 个测试文件、`146 passed`）。
- [x] 前端生产构建通过（证据：`vite build` 成功）。
- [x] 差异格式检查通过（证据：`git diff --check` 退出码为 0）。

## 端到端场景

- [x] 命中场景：任务结果包含 HTTP 400 和 `Invalid \`signature\` in \`thinking\` block` -> 展开任务后显示 Signature 错误及 Request ID（证据：纯函数和构建通过）。
- [x] 非命中场景：任务结果为其他状态码、其他错误或成功 -> 展开任务后不显示该提示（证据：过滤测试和空态逻辑通过）。

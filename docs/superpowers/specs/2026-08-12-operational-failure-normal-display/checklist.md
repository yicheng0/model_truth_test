# 巡检运行故障正常展示 Checklist

> 每项均通过运行代码或观察行为验证。

## 实现完整性

- [x] HTTP 500/502/503/504、服务暂不可用、网关错误、超时、网络连接错误、无可用渠道和额度不足被识别为运行故障（验证：`frontend/src/runsUtils.test.ts`，运行故障测试通过）。
- [x] 仅运行故障的巡检显示绿色正常状态，不显示红色异常或“需要复审”（验证：运行状态汇总单元测试通过；页面使用同一汇总结果）。
- [x] 身份探针和 Signature 探针因运行故障失败时不显示红色叉号（验证：运行故障分类与页面芯片逻辑通过构建和测试）。
- [x] 运行故障原始错误、HTTP 状态、Request ID 和标签仍保留在详情中（验证：仅调整展示分类，详情数据渲染路径未删除或改写证据字段）。
- [x] 巡检详情“参数探针”表中的 HTTP 500/503、`temporarily unavailable`、超时、网络失败和额度不足不显示红色“异常”（证据：详情状态回归测试覆盖 500、503、超时和网络错误；额度标签由统一巡检分类既有测试覆盖，均返回绿色“正常”）。
- [x] 手动探针详情与自动巡检详情使用相同状态口径（证据：两种视图均调用详情工具的同一状态文字和颜色接口，该接口已统一委托巡检分类）。
- [x] Source 请求失败的 Signature 请求日志继续显示 HTTP 状态、阶段和脱敏错误（证据：本次仅替换状态映射函数，Signature 请求日志渲染和证据字段未修改）。

## 异常边界

- [x] Kiro 身份泄漏仍被识别为真实异常（验证：`runsUtils.test.ts` 混合证据测试通过）。
- [x] HTTP 400 `Invalid signature in thinking block` 仍被识别为真实 Signature 异常（验证：运行故障排除测试通过，400 不被正常化）。
- [x] 运行故障与真实异常同时出现时由真实异常覆盖正常化（验证：混合证据测试返回 `displayState: error`）。
- [x] 普通失败或未知错误不会被一概正常化（验证：未知错误测试返回非运行故障）。
- [x] 详情页未知非运营错误仍显示红色“异常”（证据：详情状态测试中 `unexpected response shape` 返回红色“异常”）。
- [x] 详情页原生参数拒绝仍显示“参数不支持”（证据：详情状态测试中 HTTP 400 参数不支持返回金色“参数不支持”）。
- [x] Kiro 身份泄漏和明确 Signature 400 拒绝不会被详情状态复用误判为运营故障（证据：统一巡检分类既有反例测试通过，详情接口直接复用该分类）。

## 集成

- [x] 任务列表总状态、探针芯片、失败原因和复审单元使用一致的分类结果（验证：页面状态函数统一调用证据汇总/运行故障判断）。
- [x] 后端请求、评分、告警和巡检详情接口行为未被修改（验证：变更仅涉及前端展示工具、页面和测试）。
- [x] 任务列表、自动巡检详情和手动探针详情共享同一运行故障分类（证据：详情工具删除重复正则并委托任务列表使用的统一巡检工具；42 个定向测试通过）。
- [x] 响应摘要、原始错误、HTTP 状态、Request ID 和 Signature 日志均未因状态修复而隐藏或删除（证据：差异仅涉及状态工具导入、两个包装函数和测试，页面证据渲染无改动）。

## 编译与测试

- [x] 前端单元测试全部通过（验证：`cd frontend && npm test`，19 files / 151 tests passed）。
- [x] 前端生产构建通过（验证：`cd frontend && npm run build`，exit 0）。
- [x] 差异格式检查通过（验证：`git diff --check`，无输出）。
- [x] 详情页定向回归测试通过（证据：`runDetailUtils.test.ts` 与 `runsUtils.test.ts` 共 42 tests passed）。
- [x] 最新前端完整测试通过（证据：19 files / 170 tests passed）。
- [x] 最新 TypeScript 编译通过（证据：`./node_modules/.bin/tsc -b` exit 0）。
- [x] 最新前端生产构建通过（证据：`./node_modules/.bin/vite build` exit 0；仅有既有 chunk size warning）。
- [x] 最新差异格式检查通过（证据：仓库根目录运行 `git diff --check`，无输出）。
- [x] 既有未提交的删除提示与浏览器删除场景修改保持完整（证据：修复前后的既有修改文件仍在 `git status` 中，本次只新增详情工具、详情测试和已批准文档差异，没有重置或覆盖并发改动）。

## 端到端场景

- [x] 截图中的 500 `Service is temporarily unavailable` 巡检 -> 任务列表状态汇总为正常，不出现对应红色失败判断（验证：运行故障单元测试、构建通过；页面接入统一汇总）。
- [x] 同一轮网络故障 + Kiro/Signature 真实异常 -> 任务列表仍显示异常并提供复审信息（验证：混合证据测试返回 error）。
- [x] 截图中的固定身份探针 HTTP 500 -> 详情页显示“资源暂不可用”或等价非异常状态 -> 响应摘要和 Signature 请求日志仍可展开查看（证据：截图错误文本回归测试返回绿色“正常”；页面证据渲染路径未修改）。
- [x] HTTP 500 运营故障与未知协议异常各一条 -> 前者为非异常运营状态、后者仍为红色异常（证据：详情状态测试成对断言两种结果，均通过）。

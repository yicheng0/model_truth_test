# o5 缓存测试 temperature 兼容修复 Checklist

> 每项均通过运行代码或观察持久化请求证据验证。

## 实现完整性

- [x] 预热请求和全部重复请求均不包含 `temperature`（证据：缓存主流程测试检查 4 份持久化 `Result.raw_request`，顶层及参数容器均无该字段；对应 AC1）。
- [x] 缓存探针仍携带 `max_tokens`、长文本系统内容、独立探针标记和 `cache_control`（证据：缓存主流程、独立标记及完整后端回归通过；对应 AC3）。
- [x] 缓存结果仍包含缓存写入/读取 token、请求命中率、Token 命中率、请求 ID、协议和端点（证据：缓存主流程既有统计断言通过；对应 F3）。
- [x] 实现中没有 o5 模型名判断和弃用错误重试（证据：实现差异仅从缓存探针参数删除一行 `temperature: 0`；对应 N2、N3）。

## 集成

- [x] o5 兼容边界流程不再发送触发弃用错误的参数（证据：最终请求形态自动化测试通过；本次未使用真实 o5 凭据发起线上请求；对应 AC2）。
- [x] 5 分钟 TTL 原样保留（证据：缓存主流程持久化请求中的 `cache_control` 为 `{"type":"ephemeral","ttl":"5m"}`；对应 AC3）。
- [x] 1 小时 TTL 原样保留（证据：缓存聚焦回归中的一小时 TTL 用例通过；对应 AC3）。
- [x] 专用 Thinking temperature 探针仍保留参数冲突检测语义（证据：缓存与温度相关聚焦回归 `8 passed`；对应 AC4）。
- [x] 非 Anthropic Messages 兼容渠道仍被拒绝（证据：缓存相关聚焦回归包含协议限制用例并通过）。

## 编译与测试

- [x] TDD RED 已观察（证据：实现前聚焦用例失败于 `raw_request.params.temperature` 仍存在）。
- [x] 聚焦主流程测试通过（证据：`1 passed in 0.90s`）。
- [x] 缓存与温度相关回归通过（证据：`8 passed, 400 deselected in 1.34s`）。
- [x] 完整后端测试通过（证据：`417 passed in 42.70s`；对应 AC5）。
- [x] 差异无格式错误（证据：`git diff --check` 退出码为 0；对应 AC5）。
- [x] 差异范围正确（证据：后端实现差异仅为一行删除和两条断言；现有前端未提交文件未被本任务改写；对应 AC5）。

## 端到端场景

- [x] 完整流程：缓存测试预热及重复请求不携带 `temperature`，随后进入缓存统计并保存运行结果（证据：主流程自动化测试通过；未使用真实 o5 凭据补充线上验证；对应 AC1、AC2）。
- [x] 重要边界：`1h` TTL 缓存请求仍保留 TTL 和一小时缓存 token 统计（证据：一小时 TTL 聚焦用例通过；对应 AC1、AC3）。
- [x] 协议检测边界：Thinking temperature 冲突探针不受缓存探针修复影响（证据：相关聚焦回归通过；对应 AC4）。

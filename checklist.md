# 渠道总览逆向异常记录 Checklist

> 每项均通过运行代码或观察行为验证。

## 实现完整性

- [x] 包含 `kiro_identity_leak` 的报告只保留并展示 Kiro 身份泄漏异常（证据：`runsUtils.test.ts` 聚焦测试通过）。
- [x] 包含 `signature_interop_failed` 的报告只保留并展示 Signature 互验失败异常（证据：`runsUtils.test.ts` 聚焦测试通过）。
- [x] 巡检通过、Signature 通过、兼容和未知标签不进入总览异常列（证据：正常和未知标签输入返回空数组）。
- [x] 两类异常同时存在且有重复时，各展示一次并保持 Kiro 在前、Signature 在后（证据：组合异常测试返回固定顺序的两个标签）。
- [x] 空数组、空值、无报告均保持空态（证据：空输入测试通过；页面空数组分支渲染 `-`）。

## 集成

- [x] 渠道总览仍按最新报告汇总评分、等级和判断，只对“异常标签”列应用异常筛选（证据：实现差异仅修改异常标签列，生产构建成功）。
- [x] 异常列使用报告结构化标签，不读取或展示原始响应、API Key、请求头等敏感内容（证据：实现只读取 `latestReport.labels`）。
- [x] Signature 运行故障标签不会被前端补写为 `signature_interop_failed`（证据：`operational_failure` 和 `provider_error_variant` 输入返回空数组）。

## 编译与测试

- [x] 聚焦单元测试通过（证据：12/12 通过）。
- [x] 完整前端单元测试通过（证据：19 个测试文件、136/136 通过）。
- [x] 前端生产构建无错误（证据：TypeScript 编译与 Vite 构建退出码 0）。
- [x] 差异无空白错误（证据：`git diff --check` 退出码 0）。

## 端到端场景

- [x] 完整异常流程：渠道最新报告同时带有 `kiro_identity_leak`、`signature_interop_failed`、巡检通过标签和重复标签 -> 总览异常列只显示一次 Kiro 身份泄漏和一次 Signature 互验失败（证据：组合纯函数测试通过）。
- [x] 重要边界场景：渠道最新报告仅包含服务不可用、超时或 Signature 跳过/通过状态 -> 总览异常列显示 `-`，不错误归因成 Signature 互验失败（证据：非白名单标签测试返回空数组）。
- [x] 未测试渠道流程：渠道没有最新报告 -> 最新结果仍显示“待测试”，异常标签显示 `-`，原有任务操作可用（证据：既有渠道总览测试通过，页面无报告分支未变）。

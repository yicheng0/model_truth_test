# 渠道总览逆向异常记录 Spec

## 背景

“渠道测试结果总览”当前展示渠道最新报告的评分、判断和异常标签。产品需要在这个总览区域快速识别逆向或路由异常：当身份探针响应出现 Kiro 身份字段，或 Thinking Signature 跨渠道互验失败时，应直接记录为异常；Signature 互验通过等正常信息不应出现在异常标签列。

## 目标

- 在渠道总览的“异常标签”列显式展示 Kiro 身份泄漏和 Signature 互验失败。
- 该列只展示异常，不展示通过、兼容、巡检完成等正常状态信息。
- 没有异常时继续保持空态显示，不改变评分、判断、测试状态和报告详情链接。

## 功能需求

- F1: 当渠道最新测试报告的证据或异常标签表明响应出现 Kiro 身份字段时，渠道总览的异常标签列必须展示对应的 Kiro 身份泄漏异常。
- F2: 当渠道最新测试报告的 Signature 互通结果明确为失败、且不是因服务不可用等运行失败导致时，渠道总览的异常标签列必须展示 Signature 互验失败异常。
- F3: 当最新报告只包含 Signature 互通通过、Claude/官方云兼容、巡检通过或其他非异常状态时，异常标签列不得展示这些正常状态。
- F4: 当最新报告同时存在多种异常时，异常标签列应同时展示各异常；展示顺序和数量应稳定，不因重复标签产生重复显示。
- F5: 当最新报告不存在异常，或渠道尚未测试时，异常标签列继续显示空态，不影响现有“待测试”和任务操作。

## 非功能需求

- N1: 保持现有报告数据、评分、等级、测试状态和详情页行为兼容。
- N2: 异常识别必须基于已记录的结构化证据或稳定异常标签，不以模型自报身份之外的普通文本猜测替代证据。
- N3: 不新增 API Key、请求头或原始响应敏感信息的持久化和展示。
- N4: 通过前端单元测试和生产构建验证，测试应覆盖正向异常、正常过滤、组合异常和空态。

## 不做的事

- 不修改渠道评分、A-E 等级或巡检分类规则。
- 不把 Signature 互验通过记录为异常，也不把所有运行失败统一归类为 Signature 异常。
- 不改变报告详情页、告警中心或历史报告中的既有标签展示口径。
- 不新增数据库字段、迁移、后台任务或新的探针。

## 验收标准

- AC1: 准备一份包含 `kiro_identity_leak` 的最新报告后，总览对应渠道的异常标签列可见 Kiro 身份泄漏标签。
- AC2: 准备一份 Signature 状态为失败且包含 `signature_interop_failed` 的最新报告后，总览对应渠道的异常标签列可见 Signature 互验失败标签。
- AC3: 准备一份仅有 Signature 通过或巡检通过标签的报告后，总览异常标签列显示 `-`，不出现“通过”或“兼容”字样。
- AC4: 准备一份同时包含 Kiro 身份泄漏和 Signature 互验失败的报告后，总览同时显示两个异常标签且各出现一次。
- AC5: 渠道无报告或报告无异常时，总览仍显示现有空态；前端测试命令通过，生产构建命令退出码为 0。
